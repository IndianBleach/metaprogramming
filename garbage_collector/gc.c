/*
    todo:
        - start (start scanning ptrs) add tree objects, ...
        - cancel (end scanning)
        - pause (stopping )
        
        - finallize () freed objects in tree, heap
        - gen1  -> list of nodes
        - gen2  -> list of nodes
        - gen3 -> list of nodes

        - metadata
        
*/